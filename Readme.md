## To run this code:

npm install

npm run build

npm run serve

## Please refer to the branch kakao_google_sync for synchronization update while waiting for the Yandex be fully implemented by Ha
## Currently, I cannot merge the two branches because it will affect the Yandex map that cannot be showed when switching/synchronizing